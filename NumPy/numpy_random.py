import numpy as np

#Use NumPy to generate a random number between 0 and 1
rdm_num = np.random.rand(1)
print(rdm_num)

#Use NumPy to generate an array of 25 random numbers sampled from a standard normal distribution
rdm_arr = np.random.randn(25)
print(rdm_arr)